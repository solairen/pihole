all:
  children:
    linux:
      vars:
        ansible_ssh_user: vagrant
        ansible_ssh_pass: vagrant
        #ansible_ssh_private_key_file: <path_to_key>
      children:
        pihole:
          hosts:
            172.30.160.238: