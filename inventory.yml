all:
  children:
    linux:
      vars:
        ansible_ssh_user: user
        ansible_ssh_pass: password
        #ansible_ssh_private_key_file: <path_to_key>
      children:
        pihole:
          hosts:
            127.0.0.1: