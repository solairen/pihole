all:
  children:
    linux:
      vars:
        ansible_ssh_user: $USER
        ansible_ssh_pass: $PASSWORD
        ansible_port: $PORT
        ansible_ssh_private_key_file: $KEYPATH
      children:
        pihole:
          hosts:
            $PIHOLEIP:
